#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int n, m;
vector<pair<int, int>> adj[N];

bool is_bridge[N];
bool visited[N];

int main()
{
    ios_base::sync_with_stdio(0);
    cin >> n >> m;
    for (int i = 1; i <= m; i++)
    {
        int u, v;
        cin >> u >> v;
        adj[u].push_back()
    }
}